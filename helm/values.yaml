nameOverride: ""
fullnameOverride: ""
replicaCount: 1
revisionHistoryLimit: 3

image:
  repository: digital-bank-java/mfa-service
  tag: "0.0.2"
  pullPolicy: IfNotPresent

runtime:
  profile: default
  configServerUrl: http://config-server:8888

database:
  url: jdbc:postgresql://postgres:5432/mfa_service
  secretName: postgres
  usernameKey: POSTGRES_USER
  passwordKey: POSTGRES_PASSWORD

totp:
  encryptionKeySecretName: mfa-service-secrets
  encryptionKeySecretKey: MFA_TOTP_ENCRYPTION_KEY

auth:
  enabled: false
  secretName: auth-service-secrets
  secretKey: jwt-secret

service:
  type: ClusterIP
  port: 8087

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

probes:
  startup:
    path: /actuator/health/liveness
    failureThreshold: 30
    periodSeconds: 5
    timeoutSeconds: 2
  liveness:
    path: /actuator/health/liveness
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 2
  readiness:
    path: /actuator/health/readiness
    initialDelaySeconds: 10
    periodSeconds: 5
    timeoutSeconds: 2

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  fsGroup: 10001
  fsGroupChangePolicy: OnRootMismatch
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

temporaryStorage:
  sizeLimit: 64Mi

terminationGracePeriodSeconds: 30
